server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://192.168.1.6:5432/Akyuu
    username: postgres
    password: sakuya
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc.lob.non_contextual_creation: true
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 60MB

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: WARN

app:
  storage:
    local:
      basePath: uploads
  jwt:
    secret: ${AKYUU_JWT_SECRET:change-me}
